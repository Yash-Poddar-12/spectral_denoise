How to Use This File
Create and activate your Conda environment as you did before:

conda create -n spectral_env python=3.10
conda activate spectral_env


Navigate to your project's root directory in the terminal:
cd path/to/spectral_denoise

Install all packages at once using this single command:
pip install -r requirements.txt
